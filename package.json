{
  "name": "silent-dao",
  "version": "1.0.0",
  "description": "Privacy-Preserving Governance Voting on Mantle L2",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network mantleTestnet",
    "node": "hardhat node",
    "merkle": "node scripts/generateMerkleTree.js --sample"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "merkletreejs": "^0.3.11",
    "keccak256": "^1.0.6"
  }
}
