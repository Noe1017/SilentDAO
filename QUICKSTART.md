# 🚀 SilentDAO Quick Start Guide

快速启动 SilentDAO 项目的完整指南。

## 📋 前置要求

- **Node.js** >= 18.0.0
- **npm** 或 **yarn**
- **MetaMask** 浏览器扩展
- **Mantle Sepolia Testnet** 测试币

## 🛠️ 安装步骤

### Step 1: 安装根目录依赖

```bash
cd /Users/noe1017/src/SilentDAO
npm install
```

### Step 2: 安装前端依赖

```bash
cd frontend
npm install
```

### Step 3: 配置环境变量

在根目录创建 `.env` 文件：

```bash
# 复制示例文件
cp .env.example .env
```

编辑 `.env` 文件，添加你的私钥（⚠️ 注意：不要提交到 Git！）：

```
PRIVATE_KEY=你的_metamask_私钥（不含0x）
MANTLE_API_KEY=你的_mantle_api_key（可选）
```

在前端目录也创建 `.env` 文件：

```bash
cd frontend
cp .env.example .env
```

## 🏗️ 编译智能合约

```bash
# 回到根目录
cd ..

# 编译合约
npm run compile
```

## 🧪 运行测试（可选）

```bash
npm run test
```

## 🚀 部署到 Mantle 测试网

### 1. 获取测试币

- 访问 Mantle Faucet: https://faucet.sepolia.mantle.xyz/
- 连接你的 MetaMask 钱包
- 请求测试币（MNT）

### 2. 部署合约

```bash
npm run deploy
```

部署成功后，你会看到：
```
✓ SilentDAO deployed to: 0x...
✓ Network: mantleTestnet
✓ Deployment info saved to deployment.json
```

### 3. 更新前端配置

打开 `frontend/.env`，更新合约地址：

```
VITE_CONTRACT_ADDRESS=0x你的合约地址
```

## 🎨 启动前端应用

```bash
cd frontend
npm run dev
```

访问: http://localhost:3000

## 📝 使用流程

### 1. 连接钱包

- 点击右上角 "Connect Wallet"
- MetaMask 会弹出，确认连接
- 自动切换到 Mantle Sepolia Testnet

### 2. 查看提案

- 在 "Proposals" 标签页查看活跃提案
- 查看投票统计（支持/反对/弃权）

### 3. 投票

- 点击 "Vote Anonymously" 按钮
- 选择投票选项（For/Against/Abstain）
- 提交投票（你的身份是匿名的！）

### 4. 创建提案（管理员）

- 切换到 "Create Proposal" 标签
- 填写标题和描述
- 选择投票时长
- 提交提案

## 🎯 Demo 演示流程

为 Hackathon 准备的演示步骤：

### Step 1: 问题介绍（30秒）

展示传统公开投票的问题：
- 巨鲸控制
- 胁迫风险
- 参与度低

### Step 2: 创建提案（1分钟）

1. 打开前端应用
2. 连接 MetaMask 钱包
3. 创建一个新提案
4. 展示提案详细信息

### Step 3: 匿名投票（2分钟）

1. 切换到不同账户（模拟多个投票者）
2. 展示如何生成 Merkle Proof
3. 提交匿名投票
4. 展示投票计数实时更新

### Step 4: 验证结果（1分钟）

1. 展示链上投票结果
2. 强调身份隐私保护
3. 展示结果的可验证性

### Step 5: 技术亮点（30秒）

- ⚡ Mantle L2 低 Gas 费
- 🔐 Merkle Tree 验证
- ⚡ 快速确认
- 🎯 用户友好界面

## 🔧 常见问题

### Q: 部署失败 "insufficient funds"

**A:** 确保你的钱包有足够的测试币，访问: https://faucet.sepolia.mantle.xyz/

### Q: 前端无法连接合约

**A:** 检查以下几点：
1. 合约地址是否正确更新在 `frontend/.env`
2. MetaMask 是否切换到 Mantle Sepolia Testnet
3. 浏览器控制台是否有错误信息

### Q: 投票失败 "Not eligible to vote"

**A:** 你的地址不在 Merkle Tree 中。需要：
1. 更新合约的 Merkle Root
2. 或者使用测试账户中的地址

### Q: 如何添加测试账户到 Merkle Tree?

**A:** 在 `scripts/` 目录运行：
```bash
node scripts/generateMerkleTree.js
```

会生成新的 Merkle Root，然后调用合约的 `updateMerkleRoot()` 函数更新。

## 📦 项目结构

```
SilentDAO/
├── contracts/           # Solidity 智能合约
│   └── SilentDAO.sol
├── scripts/            # 部署脚本
│   └── deploy.js
├── test/              # 合约测试
│   └── SilentDAO.test.js
├── frontend/          # React 前端
│   ├── src/
│   │   ├── components/
│   │   ├── utils/
│   │   └── App.jsx
│   └── index.html
├── hardhat.config.js  # Hardhat 配置
├── package.json
└── README.md
```

## 🎨 Hackathon 展示建议

1. **准备截图/视频** - 录制关键功能演示
2. **准备 PPT** - 包含架构图和技术亮点
3. **测试流程** - 确保所有功能正常工作
4. **备用方案** - 准备演示视频以防现场网络问题

## 🏆 技术亮点（评委关注点）

- ✅ **隐私保护** - Merkle Tree 实现匿名投票
- ✅ **Gas 优化** - Mantle L2 低费用
- ✅ **可验证性** - 链上结果可验证
- ✅ **防双投** - Nullifier 机制
- ✅ **用户体验** - 直观的界面设计

## 📚 下一步

- [ ] 实现真正的 ZK 证明（使用 Circom）
- [ ] 添加更多治理功能
- [ ] 优化 Merkle Tree 生成
- [ ] 添加代币投票权重
- [ ] 部署到 Mantle 主网

## 💬 获取帮助

- **Mantle Docs**: https://docs.mantle.xyz
- **OpenZeppelin**: https://docs.openzeppelin.com
- **Ethers.js**: https://docs.ethers.org

---

**Good luck with your Hackathon! 🚀**
